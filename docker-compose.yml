version: '3.9'
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: scaling-waffle-api
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    restart: unless-stopped
