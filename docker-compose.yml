version: '3.9'
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: scaling-waffle-api
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
    restart: unless-stopped
